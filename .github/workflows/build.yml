name: Build macOS app
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  build:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Install XcodeGen
        run: brew install xcodegen
      - name: Fail if the checked-in project drifts from project.yml
        run: |
          xcodegen generate
          git diff --exit-code -- Fold.xcodeproj || {
            echo "::error::Fold.xcodeproj is stale. Run 'xcodegen generate' and commit the result."
            exit 1
          }
      - name: Build checked-in Xcode project
        run: |
          if ! xcrun metal --version; then
            xcodebuild -downloadComponent MetalToolchain
          fi
          xcodebuild -project Fold.xcodeproj -scheme Fold -configuration Release -derivedDataPath build CODE_SIGN_IDENTITY=- build
      - name: Verify animation math
        run: |
          swiftc Fold/BendMath.swift Tests/main.swift -o /tmp/fold-tests
          /tmp/fold-tests
      - uses: actions/upload-artifact@v4
        with:
          name: Fold-build
          path: build/Build/Products/Release/Fold.app
